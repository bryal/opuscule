# Expected failing vectors per build/channel against the RFC 8251 references
# (mono -> testvectorNNm.dec, stereo -> testvectorNN.dec; see run_vectors.sh).
# A vector "fails" when opus_compare rejects it.
#
# All 12 vectors pass in every config since the RFC 8251 section-9 (hybrid
# folding) and section-10 (mono phase-inversion disable) fixes landed.
# run_vectors.sh flags any change to these sets so a regression (a passer
# dropping out) or an intended improvement is updated deliberately, never
# silently. Empty string = that build/channel passes all 12.
float_mono_fail=""
float_stereo_fail=""
fixed_mono_fail=""
fixed_stereo_fail=""
